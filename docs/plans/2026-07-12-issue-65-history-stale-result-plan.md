# Issue #65：History 异步结果覆盖新日期

- 根因：日期切换后，旧请求仍可在 `await` 返回，并直接写入共享的 `sessions` 与 `runningRecords`。
- 修复：为每次日期加载分配 generation；返回后同时校验 generation 和请求日期仍为当前日期，旧结果直接丢弃。
- 测试：用延迟的旧日期请求与快速的新日期请求构造竞态，验证新日期记录保持不变。
- 风险：仅收敛 History 日记录请求，不改变月份日历加载和数据服务契约。
