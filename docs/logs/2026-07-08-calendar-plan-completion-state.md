# Calendar 今日计划误完成修复日志

- 修复 Calendar 把未训练的今日计划显示为已完成的问题。
- 完成态现在必须同时满足：计划组有 `linkedExerciseSetId`，且该 set 存在于真实训练记录。
- 清理入口覆盖云端全量替换、云端合并、本地读取今日计划、本地读取周计划。
- 脏状态会被回退为未完成，不再污染 Today、Calendar、Live Activity 起始状态。
- 正常完成计划组仍会创建训练记录，并保留完成标记。
- 新增测试：无训练记录的完成标记会被清除。
- 新增测试：有真实训练 set 的完成标记会保留。
